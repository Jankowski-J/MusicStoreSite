@model MusicStoreSite.Models.Entities.ShoppingCart

@{
    int count = Model.Products.Sum(item => item.Quantity);
    var message = "Your Cart:" + count.ToString() + " item(s)";
}

@Html.ActionLink(message, "ShoppingCart","Store")
